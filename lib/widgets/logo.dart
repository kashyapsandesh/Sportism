import 'package:flutter/material.dart';

class SportismLogo extends StatelessWidget {
  const SportismLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/sportism_logo.png',
      color: Color.fromARGB(255, 255, 255, 255),
    );
  }
}
